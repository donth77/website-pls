/**
 * Intermediate representations for the generation pipeline (MVP: stubs).
 * Replace `Record<string, unknown>` with Zod-inferred types when agents land.
 */
export type SiteSpec = Record<string, unknown>;
export type SiteBlueprint = Record<string, unknown>;
